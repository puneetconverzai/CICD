apiVersion: v1
kind: Service
metadata:
  name: cicd-poc-app-service
  namespace: cicd-poc
  labels:
    app: cicd-poc-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cicd-poc-app
